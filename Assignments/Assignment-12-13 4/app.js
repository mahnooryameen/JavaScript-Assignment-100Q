const character = "a"; // Input character

const vowels = ["a", "e", "i", "o", "u"]; // Array of vowels

// Check if the input character is in the array of vowels
const isVowel = vowels.includes(character);

document.write(isVowel); // Output: true
