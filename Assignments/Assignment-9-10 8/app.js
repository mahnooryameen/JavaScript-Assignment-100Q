var number=+prompt("enter number to check divisibility by 3");

    if (number % 3 === 0) {
      document.write(number + " is divisible by 3." + "<br>");
    } 
    else {
      document.write(number + " is not divisible by 3." + "<br>");
    }

